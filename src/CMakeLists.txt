project(TestProgram)
#-----------------------------------------------------------------------------
# Output directories.
#

set(ALL_PROGS_LIST
  TestProgram
  )

#if(BUILD_TESTING AND NOT Slicer_BUILD_${PROJECT_NAME})
#    add_subdirectory(TestSuite)
##endif()
